export function SPTOpportunityBar({
  pathname: _pathname,
  onDismissKey: _onDismissKey,
}: {
  pathname: string;
  /** stable key so dismissal persists even if query changes */
  onDismissKey?: string;
}) {
  return null;
}

