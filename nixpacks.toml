providers = []

[phases.setup]
cmds = ["corepack enable"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node artifacts/api-server/dist/index.js"
